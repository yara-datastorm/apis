- name: Deploy App 
  hosts: localhost 
  become: false
  tasks: 
  - name: English
    debug: msg="Hello World"
  - name: Espanol
    debug: msg="Holla Moundo"
  - name: "Deployment k8s"
    kubernetes.core.k8s:
      state: present
      # namespace: test
      src: ${src_k8s}

# ansible playbook testbook.yml --connection=local

# docker rm -f ansible3; docker run --name ansible3 -it -v "$WORKSPACE/playbooks:/home" ansible:1.0 ansible-playbook -i ./home/hosts ./home/deploy.yml -v

